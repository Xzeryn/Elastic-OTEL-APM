# =============================================================================
# PROCUREMENT FRONTEND - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values before building.
#
# Usage:
#   cp .env.template .env
#   # Edit .env with your values
#   docker build -t procurement-frontend .
#
# Note: These values are baked into the build at compile time.
# =============================================================================

# Elastic APM Server URL
# Get this from your Elastic Cloud deployment > APM > APM Server URL
REACT_APP_APM_SERVER_URL=https://your-cluster.apm.region.aws.found.io

# Your application domain (for distributed tracing CORS)
# This should match your Kubernetes Ingress hostname
REACT_APP_API_DOMAIN=https://your-domain.com
